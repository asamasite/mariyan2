const $=(s,p=document)=>p.querySelector(s),$$=(s,p=document)=>[...p.querySelectorAll(s)];addEventListener('load',()=>setTimeout(()=>{$('.preloader').classList.add('done');document.body.classList.remove('loading')},650));const head=$('header'),bar=$('.progress i'),topBtn=$('.quick button');function scrollState(){let y=scrollY,h=document.documentElement.scrollHeight-innerHeight;head.classList.toggle('scrolled',y>30);bar.style.width=(h?y/h*100:0)+'%';topBtn.style.opacity=y>500?'1':'.45'}addEventListener('scroll',scrollState,{passive:true});scrollState();topBtn.onclick=()=>scrollTo({top:0,behavior:'smooth'});const menu=$('.menu'),nav=$('nav');menu.onclick=()=>{let open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open)};$$('nav a').forEach(a=>a.onclick=()=>nav.classList.remove('open'));function count(el){if(el.dataset.done)return;el.dataset.done=1;let n=+el.dataset.count,t=performance.now();function go(x){let p=Math.min(1,(x-t)/900);el.textContent=Math.floor(n*p);if(p<1)requestAnimationFrame(go)}requestAnimationFrame(go)}const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');$$('[data-count]',e.target).forEach(count);io.unobserve(e.target)}}),{threshold:.13});$$('.reveal').forEach((e,i)=>{e.style.transitionDelay=(i%4)*.06+'s';io.observe(e)});$$('.hero .reveal').forEach(e=>e.classList.add('visible'));const links=$$('nav a');$$('main section[id]').forEach(s=>new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)links.forEach(a=>a.classList.toggle('active',a.hash==='#'+s.id))}),{rootMargin:'-35% 0px -55%'}).observe(s));const box=$('.lightbox'),big=$('.lightbox img');$$('.photos button').forEach(b=>b.onclick=()=>{big.src=b.querySelector('img').src;big.alt=b.querySelector('img').alt;box.classList.add('show');document.body.style.overflow='hidden'});function close(){box.classList.remove('show');document.body.style.overflow=''}$('.lightbox button').onclick=close;box.onclick=e=>{if(e.target===box)close()};addEventListener('keydown',e=>{if(e.key==='Escape')close()});let r=0,track=$('.review-track'),cards=$$('.review-track article'),counter=$('.review-nav span');function render(){track.style.transform=`translateX(-${r*(innerWidth<600?88:50)}%)`;counter.textContent=`${r+1} / ${cards.length}`}$('.prev').onclick=()=>{r=(r+cards.length-1)%cards.length;render()};$('.next').onclick=()=>{r=(r+1)%cards.length;render()};let sx=0;track.addEventListener('touchstart',e=>sx=e.touches[0].clientX,{passive:true});track.addEventListener('touchend',e=>{if(Math.abs(e.changedTouches[0].clientX-sx)>45){r=(r+(e.changedTouches[0].clientX<sx?1:-1)+cards.length)%cards.length;render()}},{passive:true});
